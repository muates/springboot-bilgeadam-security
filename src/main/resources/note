Entity
    -> User {username, password}
    -> Role {ROLE_USER, ROLE_ADMIN, ROLE_SELLER}
    ManyToMany

repo

service -> addRole addUser
controller ->

Product

enum a çevir

Product
    productName
    price

